# Local-Only Docs

This directory is intentionally ignored by git except for this README.

Use it for local review inputs, scratch notes, private feedback packets, and other working material that should stay on this machine.

Before moving anything from here into a committed docs directory, check that it is sanitized and safe to push.
